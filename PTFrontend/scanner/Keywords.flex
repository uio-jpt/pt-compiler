<YYINITIAL> {
  "template"                       { return sym(Terminals.TEMPLATE); }
  "adds"                       { return sym(Terminals.ADDS); }
  "inst"                       { return sym(Terminals.INST); }    
}
