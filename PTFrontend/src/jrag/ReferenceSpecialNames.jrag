import com.google.common.base.CharMatcher;

aspect referenceSpecialNames {

    eq TemplateMethodAccess.name() {
        return "tsuper[" + getTemplateID() + "." + getTClassID() + "]." + getID();
    }

    eq TemplateConstructorAccess.name()  {
        return "tsuper[" + getTClassID() + "]";
    }

    //    syn String ASTNode.makeNameCompilable(String name) = CharMatcher.anyOf("[].").replaceFrom(name, "$");
}
