import javaworld.Util;

aspect referenceSpecialNames {

    eq TemplateMethodAccess.name() {
        return Util.toName(getTemplateID(),getTClassID(),getID());
    }

    eq TemplateConstructorAccess.name()  {
        return Util.toName(getTemplateID(),getTClassID());        
    }
}
