build: ant build
run test: ant run

----

Fixes TODO:

Merging:

sjekk etter merge kollisjon på signatur nivå. (per i dag på navn nivå)

renaming pga merge_conflikt: ha med templatnavn.

if merging occurs, demand own constructor (allowed to call
super[className] in the beginning).

