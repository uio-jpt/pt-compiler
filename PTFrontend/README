build: ant build
run test: ant run

----

Fixes TODO:

Merging:

* merge renaming; ha med template navn og orgKlassenavn for spesifitet.
* konsistens med navn på merga metoder/konstruktører og kall på dem.
* sjekke på at merga metoder/konstruktører bare kan kalles fra "riktig" sted.
* greppe source etter TODO
* veldig mye annet småplukk.

Til diskusjon/ettertanke:
* sjekk etter merge kollisjon på signatur nivå. (per i dag på navn nivå)

PTDummyRename:
    Skal man tillate dette (?):
    inst T with A => X, B => X (getX() -> getY());
    inst T with A => X, B => X (getX(*) -> getY(*));
    inst T with A => X, B => X (getX(int) -> getY(int));
